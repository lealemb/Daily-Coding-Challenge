/**
join( )
This method returns a new string by concatenating all of the array’s
elements separated by the specified separator.
**/

const arr = ["m", "a", "n", "d", "e", "e", "p"]; 
console.log(arr.join('')); // mandeep
